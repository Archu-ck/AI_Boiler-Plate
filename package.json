{
  "name": "hackathon-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "features/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
